[package]
name = "prime-count"
version = "0.1.0"
edition = "2021"

[dependencies]
primal = "0.3"
rayon = "1.10"
crossterm = "0.28"
mimalloc = { version = "0.1", default-features = false }

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
